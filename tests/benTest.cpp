/* Every header files working in progress here */
#include "../include/public_header.hpp"
#include "../src/stock.cpp"

int main(){
    /* Stock* s = new Stock("Ben", 200.00);
    Company* c = new Company("Ben's");
    Company* a = new Company("Anna's");
    //s->PrintStock();
    s->SetCompany(c);
    s->PrintCompany();
    s->SetCompany(a);
    s->PrintCompany();
    delete s;
    delete c;
    s = NULL;
    c = NULL;*/

    Transaction t;
    cout << "Current Time: " << t.currentTime << endl;

    return 0;
}
